[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short

# Redirect .pytest_cache out of the project root
cache_dir = .cache/pytest

markers =
    integration: requires a live MongoDB instance (auto-skipped when unavailable)
