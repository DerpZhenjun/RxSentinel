[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short

# Keep pytest cache under tests/ (avoids clutter at project root)
cache_dir = tests/.pytest_cache

markers =
    integration: requires a live MongoDB instance (auto-skipped when unavailable)
