# =============================================================================
# requirements-test.txt — 测试 / CI 专用依赖
# =============================================================================
#
# 适用场景：运行 pytest 单元测试、E2E 测试、CI 流水线
#
#   pip install -r requirements-test.txt
#
# 与 requirements.txt 的关系：
#   本文件是 requirements.txt 的轻量子集，故意排除了运行时中的重型依赖
#   （streamlit、matplotlib、openai、ollama 等），以加快 CI 安装速度。
#   tqdm 保留：`ai_processor_common.run_platform_pipeline` 与 e2e 管线测试依赖。
#
#   ┌─────────────────────────────────────────────────────┐
#   │  只想运行测试          → 仅安装本文件               │
#   │  只想运行应用          → 仅安装 requirements.txt    │
#   │  两者都要（本地全量）  → 两个文件都安装             │
#   │  使用爬虫功能          → 另装 MediaCrawler/requirements.txt │
#   └─────────────────────────────────────────────────────┘
#
# =============================================================================

fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27          # FastAPI TestClient 依赖
pymongo>=4.0
pydantic>=2.0
python-dotenv>=1.0
requests>=2.31
slowapi>=0.1.9
tqdm>=4.66           # AI 流水线进度条；tests/e2e/test_pipeline_output.py

# --- 测试工具 ---
pytest>=7.4
pytest-cov>=7.0
